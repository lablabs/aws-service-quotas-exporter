linters:
  disable-all: true
  enable:
    - gosimple
    - staticcheck
    - gofmt
    - govet
    - revive
    - unused

linters-settings:
  staticcheck:
    checks:
      - '-SA5008'
  gofmt:
    # simplify code: gofmt with `-s` option, true by default
    simplify: true